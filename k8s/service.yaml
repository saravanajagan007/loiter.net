apiVersion: v1
kind: Service
metadata:
  name: loiter-service
  namespace: default
spec:
  selector:
    app: loiter
    tier: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
